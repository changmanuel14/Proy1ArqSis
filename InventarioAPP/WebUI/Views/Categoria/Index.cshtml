@model IEnumerable<Models.Categoria>

@{
    ViewBag.Title = "Index";
}
<section class="content-header">
    <h2>Categoría</h2>
 </section>

<section class="content">

    <div class="box">
        <div class="box-body">
            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayName("Nombre")
                    </th>
                    <th>
                        @Html.DisplayName("Fecha de Creación")
                    </th>
                    <th>
                        @Html.DisplayName("Estado")
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.nombre_categoria)
                        </td>
                        <td>
                            @Convert.ToDateTime(item.fecha_creacion).ToString("dd/MM/yyyy")
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.estado_categoria)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.CategoriaId }) |
                            @Html.ActionLink("Details", "Details", new { name = item.nombre_categoria }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.CategoriaId })
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>

</section>
